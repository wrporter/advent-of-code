{
  "name": "advent-of-code",
  "scripts": {
    "run-file": "node -r ts-node/register/transpile-only -r tsconfig-paths/register",
    "start": "node -r ts-node/register/transpile-only -r tsconfig-paths/register run.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "ts-array-extensions": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.10",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.1",
    "typescript": "^4.9.3",
    "vite-tsconfig-paths": "^3.6.0",
    "vitest": "^0.25.3"
  }
}
